import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pathfinder2 Fights Companion',
  webDir: 'dist'
};

export default config;
